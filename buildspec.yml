version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  install:
    commands:
      - pip install aws-sam-cli
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
      - sam package --template-file template.yaml --s3-bucket grimlocks-testing --output-template-file packaged.yaml
      # - run tests here 
      - sam deploy --template-file packaged.yaml --stack-name WTFITS --capabilities CAPABILITY_IAM
  #post_build:
    #commands:
      # - command
#artifacts:
  #files:
    # - location
    # - location
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths
